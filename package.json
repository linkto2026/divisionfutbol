{
  "name": "divisionfutbol",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx tinacms dev -c \"npx @11ty/eleventy --serve\"",
    "build": "npx tinacms build && npx @11ty/eleventy",
    "build:eleventy": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "@tinacms/cli": "^2.5.1",
    "tinacms": "^3.9.3"
  }
}
